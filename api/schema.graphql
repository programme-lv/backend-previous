schema {
    query: Query
    mutation: Mutation
}

type Query

type Mutation {
  executeCode(code: String!, languageID: ID!): ExecutionResult!
}

type ExecutionResult {
  stdout: String!
  stderr: String!
}
