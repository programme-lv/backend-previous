type Query {
  login(username: String!, password: String!): PublicUser!
}

type Mutation {
  register(
    username: String!
    password: String!
    email: String!
    firstName: String!
    lastName: String!
  ): PublicUser!
}

type PublicUser {
  id: ID!
  username: String!
}
